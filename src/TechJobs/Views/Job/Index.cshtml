@{
//  #2.1- declare the ViewModel (outside of this @{ } block)
}
@model  TechJobs.Models.Job

<table class="job-listing">
    @{ 
//  #2.2 - display the job fields, one per row (outside of this @{ } block)
    }

    
    <tr>
        <td>@Model.Name</td>
    </tr>
    <tr>
        <td>@Model.Employer.Value</td>
    </tr>
    <tr>
        <td>@Model.Location.Value</td>
    </tr>
    <tr>
        <td>@Model.CoreCompetency.Value</td>
    </tr>
    <tr>
        <td>@Model.PositionType.Value</td>
    </tr>


</table>